name: build-agent-service-manager-image
on:
  push:
    branches:
      - 'v2.*'
    paths:
      - .github/workflows/build-agent-service-manager-image.yml
      - 07-agent-service-manager-service/image_build/agent-service-manager/*
      - 07-agent-service-manager-service/image_build/agent-service-manager-frontend/*
      - 07-agent-service-manager-service/image_build/agent-service-manager-frontend.Dockerfile
jobs:
  build-agent-service-manager-image:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.repository_owner }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push agent-service-manager-image
        uses: docker/build-push-action@v5
        with:
          context: 07-agent-service-manager-service/image_build/agent-service-manager/
          platforms: linux/amd64,linux/arm64
          push: true
          tags: |
            runshine0819/agent-service-manager:latest
            ghcr.io/runshine/agent-service-manager:latest
          file: 07-agent-service-manager-service/image_build/agent-service-manager/Dockerfile

  build-agent-service-manager-frontend-image:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: 'true'

      - name: clean to default
        run: sed -i 's/https:\/\/develop-agentserver.819819.xyz//g'   07-agent-service-manager-service/image_build/agent-service-manager-frontend/api.ts

      - name: cat api.ts
        run: cat 07-agent-service-manager-service/image_build/agent-service-manager-frontend/api.ts

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.repository_owner }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push agent-service-manager-frontend-image
        uses: docker/build-push-action@v5
        with:
          context: 07-agent-service-manager-service/image_build/agent-service-manager-frontend
          platforms: linux/amd64,linux/arm64
          push: true
          tags: |
            runshine0819/agent-service-manager-frontend:latest
            ghcr.io/runshine/agent-service-manager-frontend:latest
          file: 07-agent-service-manager-service/image_build/agent-service-manager-frontend.Dockerfile
