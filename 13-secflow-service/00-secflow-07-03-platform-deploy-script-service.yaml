apiVersion: v1
kind: Service
metadata:
  name: secflow-platform-deploy-script
  namespace: secflow-ns
spec:
  type: ClusterIP
  selector:
    name: "secflow-platform-deploy-script"
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: secflow-platform-deploy-script