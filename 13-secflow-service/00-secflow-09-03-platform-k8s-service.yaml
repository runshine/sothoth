apiVersion: v1
kind: Service
metadata:
  name: secflow-platform-k8s
  namespace: secflow-ns
spec:
  type: ClusterIP
  selector:
    name: "secflow-platform-k8s"
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
      name: secflow-platform-k8s