apiVersion: apps/v1
kind: Deployment
metadata:
  name: secflow-platform-static-binary
  namespace: secflow-ns
spec:
  replicas: 3
  selector:
    matchLabels:
      name: secflow-platform-static-binary
  template:
    metadata:
      labels:
        name: secflow-platform-static-binary
    spec:
      dnsPolicy: ClusterFirstWithHostNet
      containers:
      - name: secflow-platform-static-binary
        image: ghcr.io/runshine/secflow-platform-static-binary:latest  # 包含kubectl命令
        imagePullPolicy: Always
        ports:
        - containerPort: 8080
        env:
          - name: MYSQL_SERVER_ADDRESS
            value: mysql+pymysql://secflow:Huawei12%23%24@mysql.sothothv2-ns.svc.cluster.local/secflow?ssl=false
          - name: PACKAGE_ROOT_DIR
            value: /data
        volumeMounts:
        - name: secflow-platform-static-binary-nfs-pvc
          mountPath: /data
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /api/packages/health
            port: 8080
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /api/packages/health
            port: 8080
          initialDelaySeconds: 5
          periodSeconds: 5

      - name: nginx
        image: nginx:alpine
        imagePullPolicy: Always
        ports:
          - containerPort: 80
        volumeMounts:
          - name: secflow-platform-static-binary-nginx-config
            mountPath: /etc/nginx/nginx.conf
            subPath: nginx.conf
          - name: secflow-platform-static-binary-nfs-pvc
            mountPath: /data
        livenessProbe:
          httpGet:
            path: /healthz
            port: 80
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /healthz
            port: 80
          initialDelaySeconds: 5
          periodSeconds: 5
        # 资源限制
        resources:
          requests:
            memory: "64Mi"
            cpu: "250m"
          limits:
            memory: "128Mi"
            cpu: "500m"

      volumes:
      - name: secflow-platform-static-binary-nfs-pvc
        persistentVolumeClaim:
          claimName: secflow-platform-static-binary-nfs-pvc  # 引用PVC
      - name: secflow-platform-static-binary-nginx-config
        configMap:
          name: secflow-platform-static-binary-nginx-config
          items:
            - key: nginx.conf
              path: nginx.conf
