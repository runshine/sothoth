apiVersion: apps/v1
kind: Deployment
metadata:
  name: secflow-app-system-analyse
  namespace: secflow-ns
spec:
  replicas: 1
  selector:
    matchLabels:
      name: secflow-app-system-analyse
  template:
    metadata:
      labels:
        name: secflow-app-system-analyse
    spec:
      dnsPolicy: ClusterFirstWithHostNet
      containers:
      - name: secflow-app-system-analyse
        image: ghcr.io/runshine/secflow-app-system-analyse:latest
        imagePullPolicy: Always
        env:
          - name: SECFLOW_SYSTEM_ANALYSE_ROLE
            value: api
          - name: TZ
            value: Asia/Shanghai
          - name: PORT
            value: "8080"
          - name: FILESERVER_ROOT
            value: /data/files
          - name: SECFLOW_SYSTEM_ANALYSE_DB_AUTO_MIGRATE
            value: "true"
          - name: SECFLOW_SYSTEM_ANALYSE_DB_POOL_SIZE_API
            value: "5"
          - name: SECFLOW_SYSTEM_ANALYSE_DB_MAX_OVERFLOW_API
            value: "5"
          - name: SECFLOW_SYSTEM_ANALYSE_DB_POOL_TIMEOUT_API
            value: "10"
          - name: SECFLOW_SYSTEM_ANALYSE_DB_POOL_RECYCLE_API
            value: "1800"
        ports:
        - containerPort: 8080
        volumeMounts:
        - name: secflow-app-system-analyse-config
          mountPath: /app/service.yaml
          subPath: service.yaml
        - name: secflow-platform-fileserver-data-nfs-pvc
          mountPath: /data
        resources:
          requests:
            memory: "16Gi"
            cpu: "4"
          limits:
            memory: "32Gi"
            cpu: "8"
        livenessProbe:
          httpGet:
            path: /api/app/system-analyse/livez
            port: 8080
          initialDelaySeconds: 30
          periodSeconds: 30
          timeoutSeconds: 60
        readinessProbe:
          httpGet:
            path: /api/app/system-analyse/readyz
            port: 8080
          initialDelaySeconds: 15
          periodSeconds: 15
          timeoutSeconds: 15
          failureThreshold: 5
        startupProbe:
          httpGet:
            path: /api/app/system-analyse/livez
            port: 8080
          initialDelaySeconds: 10
          periodSeconds: 10
          timeoutSeconds: 15
          failureThreshold: 60
      volumes:
      - name: secflow-app-system-analyse-config
        configMap:
          name: secflow-app-system-analyse-config
          items:
            - key: service.yaml
              path: service.yaml
      - name: secflow-platform-fileserver-data-nfs-pvc
        persistentVolumeClaim:
          claimName: secflow-platform-fileserver-data-nfs-pvc
