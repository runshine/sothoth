apiVersion: v1
kind: Service
metadata:
  name: secflow-app-binary-to-source-manager
  namespace: secflow-ns
spec:
  type: ClusterIP
  selector:
    name: "secflow-app-binary-to-source-manager"
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
      name: secflow-app-binary-to-source-manager
