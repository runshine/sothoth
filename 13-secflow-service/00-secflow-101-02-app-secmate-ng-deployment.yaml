apiVersion: apps/v1
kind: Deployment
metadata:
  name: secflow-app-secmate-ng
  namespace: secflow-ns
spec:
  replicas: 1
  selector:
    matchLabels:
      name: secflow-app-secmate-ng
  template:
    metadata:
      labels:
        name: secflow-app-secmate-ng
    spec:
      serviceAccountName: secflow-app-secmate-ng-serviceaccount
      dnsPolicy: ClusterFirstWithHostNet
      containers:
      - name: secflow-app-secmate-ng
        image: ghcr.io/runshine/secflow-app-secmate-ng:latest  # 包含kubectl命令
        imagePullPolicy: Always
        ports:
        - containerPort: 8080
        volumeMounts:
        - name: secflow-app-secmate-ng-config
          mountPath: /app/config.yaml
          subPath: config.yaml
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /api/app/secmate-ng/health
            port: 8080
          initialDelaySeconds: 30
          periodSeconds: 30
        readinessProbe:
          httpGet:
            path: /api/app/secmate-ng/health
            port: 8080
          initialDelaySeconds: 30
          periodSeconds: 30

      volumes:
      - name: secflow-app-secmate-ng-config
        configMap:
          name: secflow-app-secmate-ng-config
          items:
            - key: config.yaml
              path: config.yaml
