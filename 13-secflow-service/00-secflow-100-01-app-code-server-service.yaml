apiVersion: v1
kind: Service
metadata:
  name: secflow-app-code-server
  namespace: secflow-ns
spec:
  type: ClusterIP
  selector:
    name: "secflow-app-code-server"
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
      name: secflow-app-code-server