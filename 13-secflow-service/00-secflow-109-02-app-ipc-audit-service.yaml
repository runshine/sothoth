apiVersion: v1
kind: Service
metadata:
  name: secflow-app-ipc-audit
  namespace: secflow-ns
spec:
  type: ClusterIP
  selector:
    name: secflow-app-ipc-audit
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
      name: secflow-app-ipc-audit
