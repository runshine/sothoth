apiVersion: apps/v1
kind: Deployment
metadata:
  name: secflow-platform-system-analysis
  namespace: secflow-ns
spec:
  replicas: 2
  selector:
    matchLabels:
      name: secflow-platform-system-analysis
  template:
    metadata:
      labels:
        name: secflow-platform-system-analysis
    spec:
      dnsPolicy: ClusterFirstWithHostNet
      containers:
      - name: secflow-platform-system-analysis
        image: ghcr.io/runshine/secflow-platform-system-analysis:latest
        imagePullPolicy: Always
        env:
          - name: TZ
            value: Asia/Shanghai
          - name: CONFIG_PATH
            value: /app/config.yaml
        ports:
        - containerPort: 8080
        volumeMounts:
        - name: secflow-platform-system-analysis-config
          mountPath: /app/config.yaml
          subPath: config.yaml
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /api/system-analysis/health
            port: 8080
          initialDelaySeconds: 30
          periodSeconds: 30
        readinessProbe:
          httpGet:
            path: /api/system-analysis/health
            port: 8080
          initialDelaySeconds: 15
          periodSeconds: 15
      volumes:
      - name: secflow-platform-system-analysis-config
        configMap:
          name: secflow-platform-system-analysis-config
          items:
            - key: config.yaml
              path: config.yaml
