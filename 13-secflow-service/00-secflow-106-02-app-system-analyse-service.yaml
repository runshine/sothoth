apiVersion: v1
kind: Service
metadata:
  name: secflow-app-system-analyse
  namespace: secflow-ns
spec:
  type: ClusterIP
  selector:
    name: "secflow-app-system-analyse"
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
      name: secflow-app-system-analyse
