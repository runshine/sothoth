apiVersion: apps/v1
kind: Deployment
metadata:
  name: secflow-platform-k8s
  namespace: secflow-ns
spec:
  replicas: 1
  selector:
    matchLabels:
      name: secflow-platform-k8s
  template:
    metadata:
      labels:
        name: secflow-platform-k8s
    spec:
      serviceAccountName: secflow-platform-k8s-serviceaccount
      dnsPolicy: ClusterFirstWithHostNet
      containers:
      - name: secflow-platform-k8s
        image: ghcr.io/runshine/secflow-platform-k8s:latest  # 包含kubectl命令
        imagePullPolicy: Always
        ports:
        - containerPort: 8080
        volumeMounts:
        - name: secflow-platform-k8s-config
          mountPath: /app/config.yaml
          subPath: config.yaml
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /api/k8s/health
            port: 8080
          initialDelaySeconds: 30
          periodSeconds: 30
        readinessProbe:
          httpGet:
            path: /api/k8s/ready
            port: 8080
          initialDelaySeconds: 30
          periodSeconds: 30

      volumes:
      - name: secflow-platform-k8s-config
        configMap:
          name: secflow-platform-k8s-config
          items:
            - key: config.yaml
              path: config.yaml
