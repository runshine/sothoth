{
  "kind": "cli",
  "description": "认证 CLI 上报示例",
  "command": "curl -X POST \"$SECFLOW_VULN_BASE/api/vuln/public/intake/submissions\" -H 'Authorization: Bearer $SECFLOW_TOKEN' -H 'Content-Type: application/json' --data @payload.json",
  "payload_file": "payload-template.json",
  "notes": [
    "必须携带 project_id",
    "必须携带 Authorization: Bearer <token>",
    "created_by_type 会自动写为 human"
  ]
}
