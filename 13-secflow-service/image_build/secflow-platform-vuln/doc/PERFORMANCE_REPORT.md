# SecFlow 漏洞生命周期编排引擎测速报告

测试时间：

- `2026-03-22T11:49:38Z`

测试环境：

- 模式：本地 ASGI 直连
- 数据库：临时 sqlite 文件
- 认证/项目权限：测试 override
- 预置数据：40 个 `Case`

## 1. 测试场景与结果

| 场景 | 请求数 | 并发 | 吞吐量 RPS | avg_ms | p95_ms | max_ms | 错误数 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| `health_read` | 100 | 20 | 2786.95 | 0.34 | 0.46 | 0.92 | 0 |
| `case_create` | 40 | 5 | 117.11 | 38.45 | 73.39 | 74.52 | 0 |
| `case_list` | 60 | 15 | 201.27 | 70.46 | 82.37 | 88.59 | 0 |
| `dashboard_overview` | 60 | 15 | 187.27 | 74.20 | 106.15 | 108.89 | 0 |
| `stateful_case_flow` | 15 | 1 | 31.85 | 22.32 | 25.79 | 32.45 | 0 |

## 2. 结果解读

- 基础健康读接口非常轻量，适合探针与高频轮询。
- `Case` 创建在当前本地 sqlite 基准下表现稳定，没有出现写入错误或阶段初始化异常。
- `case_list` 和 `dashboard_overview` 在中等并发下稳定返回，没有出现聚合错误。
- 最小状态流转闭环没有出现状态错乱、重复任务或回调异常。
- 所有场景错误数均为 `0`。

## 3. 当前结论

从当前实现看：

- 功能正确性通过
- 基础性能稳定
- 在工作台级典型访问模式下没有暴露新的逻辑错误

## 4. 说明

这份测速结果用于验证应用层设计和接口逻辑，不代表最终生产压测上限。

原因：

- 当前基准使用本地 ASGI 直连
- 存储使用 sqlite，而不是生产 MySQL
- 未经过 ingress、k8s Service、容器网络和真实能力服务回调链路

后续若需要生产级压测，建议在 k8s 环境中对以下路径继续做：

- `/api/vuln/cases`
- `/api/vuln/cases/ops/dashboard/overview`
- `/api/vuln/actions/ops/queue`
- 外部 `Action -> callback -> case detail` 闭环
