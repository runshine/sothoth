请整理反编译优化的输出结果。

## 步骤

### 1. 扫描反编译输出

扫描 `{working_dir}/docker_output/` 目录，列出所有反编译源码文件。

### 2. 提取上下文信息

从输入任务描述中提取**模块名称**，后续阶段需要这个信息。

### 3. 写入总结报告

将以下内容写入 `{summary_path}`：

- 反编译状态
- 反编译源码目录路径: `{working_dir}/docker_output`
- 源码文件列表
- 文件类型和数量统计

### 4. 生成下阶段任务文件

在 `{results_dir}/` 下创建 **一个** 文件 `result_001.md`，内容必须严格遵循以下格式：

```markdown
# 外部数据入口分析任务

## 输入
- 输入文件夹: {working_dir}/docker_output
- 内容描述: 优化后的高危模块反编译源码
- 模块名称: <从输入任务中提取的模块名称>

## 上下文信息（请传递给所有下游阶段）
- 反编译源码目录: {working_dir}/docker_output

## 源码文件列表
（此处列出主要的源码文件清单）

## 任务说明
分析反编译源码中所有接收外部数据输入的函数入口（如网络消息处理函数、文件解析入口、
IPC 消息处理器等），为每个外部消息处理函数入口生成一份分析报告。
```

> **重要**:
> - `上下文信息` 中的 `反编译源码目录` 路径至关重要，后续的数据流分析和漏洞挖掘阶段都需要它
> - 必须使用绝对路径
