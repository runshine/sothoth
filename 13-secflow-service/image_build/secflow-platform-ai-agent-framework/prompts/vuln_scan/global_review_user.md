请对以下基于数据流分析的漏洞挖掘报告进行全面质量评审。

---

## 原始任务

{task}

---

## Worker 提交的总结报告

{summary}

---

## 结果文件列表

{results}

---

## 评审要点

请基于你的评审标准，重点核实以下问题：

### 数据流利用度
- Worker 在 summary.md 的路径覆盖度表中列出了多少条路径？是否覆盖了数据流分析的所有终点？
- 多少个 EXPORT 终点被实际跟入分析？多少个被跳过？
- 数据流分析的关键发现（★ 标记）是否全部有验证结论？

### 源码分析深度
- Worker 的结论是基于实际源码阅读，还是仅复述数据流分析内容？
- 漏洞报告中是否引用了源代码片段？引用是否充分（≥5 行上下文）？
- Worker 是否发现了数据流分析未标注的额外问题？

### 报告质量
- 每个漏洞报告是否有从 INPUT 到危险操作的完整攻击路径？
- 触发条件是否具体到字段和取值范围？
- 影响评估是否区分了不同后果类型（RCE / 任意写 / DoS 等）？
- 多个报告之间是否存在分析雷同、模板化的现象？

### summary.md 完整性
- 是否包含了分析方法论、路径覆盖度表、关键发现验证、漏洞汇总表、风险评估、局限性说明等全部章节？

请严格按照评审标准输出 JSON 结果。

**注意：禁止写入任何文件。** 只需在你的回复中输出 JSON 结果即可。可以使用 read/bash(grep,readelf等只读命令) 辅助分析，但不要 write/edit 任何文件。
