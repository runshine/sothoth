apiVersion: v1
kind: Service
metadata:
  name: chirmera-platform-schedule
  namespace: secflow-ns
  labels:
    app: chirmera-platform-schedule
spec:
  type: ClusterIP
  selector:
    app: chirmera-platform-schedule
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP
