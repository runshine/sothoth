apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: chirmera-platform-schedule-worker
  namespace: secflow-ns
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: chirmera-platform-schedule-worker
  minReplicas: 2
  maxReplicas: 20
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
