apiVersion: v1
kind: Service
metadata:
  name: secflow-platform-system-analysis
  namespace: secflow-ns
spec:
  type: ClusterIP
  selector:
    name: "secflow-platform-system-analysis"
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
      name: secflow-platform-system-analysis
