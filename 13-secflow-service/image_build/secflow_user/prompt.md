13-secflow-service/image_build/secflow_user是一个文件夹是一个独立的web微服务，使用python语言，请实现下列功能：
1、该微服务对外提供用户管理、认证等功能，提供用户的增、删、查、改功能，提供角色功能，支持用户绑定一个或者多个角色，支持用户角色信息的查询，数据持久化到数据库中；微服务的行为通过机机之间的token进行认证，同时提供根据用户、密码换人机token的服务（人机token有效期）
2、请提供完整的dockerfile文件和依赖的文件
3、请提供完整的doc文档，文档放在单独的doc文件夹下
4、后台持久化使用mysql数据库，启动时有一个配置文件的运行参数，请提供配置参数，不要使用环境变量，而是配置文件
5、该微服务运行在K8S环境中，请提供deployment的多实例部署和基于clusterip的service配置文件
6、要提供人机Token的鉴权API，机机Token仅用于自身API外部访问的鉴权，鉴权人机Token时，要判断是否有效，是否在有效期

请精简README.md，已经有独立的doc文件夹来存储文档了