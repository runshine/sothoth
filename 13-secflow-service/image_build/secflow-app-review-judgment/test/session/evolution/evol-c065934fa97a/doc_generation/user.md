请阅读 `/data/files/44f9029d00650a10/DATAFLOW_VULN_SCANNER/agent-state/evolution/evol-c065934fa97a/doc_generation/context.json` 中的 JSON 上下文，并按要求生成 4 份 Markdown。

注意：
- 这是给 agent 注入的技能文档，不是给人写的长报告。
- worker 文档偏“执行策略”；三个 advisor 文档分别偏“覆盖面评审 / 深度评审 / 结果真伪评审”。
- 如果已有漏洞结果已经呈现某些模式，应在文档中体现“延续这些线索，扩相邻路径，补同类证据”的意识。
- 如果是第 N 轮且带有 adjustment_direction / previous_results_summary，要体现本轮调整。

只输出 JSON 对象本身。
