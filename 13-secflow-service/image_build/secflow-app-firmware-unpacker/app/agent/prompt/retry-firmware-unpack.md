---
description: Resume firmware unpacking based on reviewer's reason, update summary.txt when done
---
Use the firmware-unpacker agent to re-analyze the firmware in $input. The previous unpacking had issues — read `$output/reason.txt` for details, address each problem, and update `$output/summary.txt` with the new findings. The unpacking results should still be saved in $output.
