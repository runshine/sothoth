service_id: secflow-app-poc-gen-verify
service_name: PoC 生成与验证服务
api_prefix: /api/app/poc-gen-verify
host: secflow-app-poc-gen-verify
port: 80
maturity: alpha
description: "PoC 生成与 GDB 验证服务 — 从漏洞报告 + 数据流入口函数生成 PoC 并在 GDB 下触发验证"

database:
  host: "mysql.sothothv2-ns.svc.cluster.local"
  port: 3306
  username: "secflow"
  password: "Huawei12#$"
  name: "secflow"
  pool_size: 5
  max_overflow: 10

registry:
  enabled: true
  menu_service_url: "http://secflow-platform-menu:80"
  service_id: secflow-app-poc-gen-verify
  service_name: "PoC 生成与验证"
  host: secflow-app-poc-gen-verify
  port: 80
  maturity: alpha
  description: "PoC 生成与 GDB 验证服务"
  api_prefix: /api/app/poc-gen-verify
  unregister_on_shutdown: false
  heartbeat_interval_seconds: 30
  menu:
    id: app-poc-gen-verify
    path: /app/poc-gen-verify
    icon: bug
    order: 115
    level1:
      name: "应用工具"
      name_en: "App Tools"
    level2:
      name: "PoC 生成与验证"
      name_en: "PoC Gen & Verify"
    level3:
      name: ""
      name_en: ""

app:
  host: "0.0.0.0"
  port: 8080
  debug: false
engine:
  # 漏洞判定引擎契约（Contract v2.3）。name 必须与平台管理员预注册的 engine_name 一致。
  name: secflow-app-poc-gen-verify
  version: "1.0.0"
  endpoint_prefix: /api/app/poc-gen-verify/intake
  heartbeat_url: "http://secflow-platform-vuln/api/vuln/internal/vuln-confirm/engines/heartbeat"
  heartbeat_interval_seconds: 30
  heartbeat_request_timeout_seconds: 5.0
  results_push_url: "http://secflow-platform-vuln/api/vuln/internal/vuln-confirm/results/push"
  results_push_timeout_seconds: 5.0
  vuln_categories_url: "http://secflow-platform-vuln/api/vuln/vuln-categories"
  vuln_categories_timeout_seconds: 5.0
  vuln_categories_cache_ttl_seconds: 1800
  # 路径A（GDB 触发崩溃 = 内存安全类）默认分类候选；推送前经接口6目录校验存在才填
  emit_confirmed_category: true
  default_confirmed_category: "内存安全类型"
