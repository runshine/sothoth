apiVersion: v1
kind: Secret
metadata:
  name: secflow-app-kernel-scan-secret
  namespace: secflow-ns
type: Opaque
stringData:
  # Replace before applying, or create the secret manually with kubectl.
  ANTHROPIC_API_KEY: "sk-REPLACE_ME"
  # Optional. Keep empty for the default API endpoint.
  ANTHROPIC_BASE_URL: ""
