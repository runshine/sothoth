apiVersion: v1
kind: Service
metadata:
  name: secflow-app-kernel-scan
  namespace: secflow-ns
spec:
  type: ClusterIP
  selector:
    name: secflow-app-kernel-scan
  ports:
    - name: secflow-app-kernel-scan
      port: 80
      targetPort: http
      protocol: TCP
