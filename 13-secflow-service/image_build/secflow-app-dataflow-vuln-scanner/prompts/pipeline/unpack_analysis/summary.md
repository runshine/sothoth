请整理解包分析的输出结果。

## 步骤

### 1. 扫描 Docker 输出

扫描 `{working_dir}/docker_output/` 目录，获取完整的文件和目录列表。

### 2. 写入总结报告

将以下内容写入 `{summary_path}`：

- 解包状态（成功/失败）
- 输出目录路径
- 文件类型统计（可执行文件、库文件、配置文件等）
- 目录结构概览

### 3. 生成下阶段任务文件

在 `{results_dir}/` 下创建 **一个** 文件 `result_001.md`，内容必须严格遵循以下格式：

```markdown
# 系统分析任务

## 输入
- 输入文件夹: {working_dir}/docker_output
- 内容描述: 已完整解包的产品软件/固件内容

## 文件概览
（此处列出主要的文件和子目录清单）

## 任务说明
对解包后的完整系统文件进行分析，识别其中的高危二进制模块（如网络服务、驱动程序、加密模块等），
将每个高危模块放置在输出文件夹的独立子目录中。
```

> **重要**: `result_001.md` 的内容将直接作为下一阶段（系统分析）的输入任务描述文件。
> 请确保其中的路径使用绝对路径，文件概览信息完整准确。
