请整理数据流分析的输出结果。

## 步骤

### 1. 扫描数据流分析输出

扫描 `{working_dir}/docker_output/` 目录，找到数据流分析结果文件。

### 2. 提取上下文信息

从输入任务描述中提取以下信息（必须传递给下游阶段）：
- **反编译源码目录**（从"上下文信息"章节获取）
- **模块名称**
- **函数名称**

### 3. 写入总结报告

将以下内容写入 `{summary_path}`：

- 分析状态
- 数据流分析结果文件路径
- 分析的目标函数信息
- 发现的数据流路径数量概要

### 4. 生成下阶段任务文件（漏洞挖掘）

在 `{results_dir}/` 下创建 **一个** 文件 `result_001.md`，内容必须严格遵循以下格式：

```markdown
# 漏洞挖掘任务

## 输入
- 数据流分析文件: {working_dir}/docker_output/<数据流分析结果文件名>
- 反编译源码目录: <从输入任务中继承的反编译源码目录绝对路径>
- 内容描述: 该函数入口的数据流分析结果和对应模块的反编译源码

## 上下文信息
- 模块名称: <从输入任务中继承的模块名称>
- 函数名称: <从输入任务中继承的函数名称>
- 数据流分析文件: {working_dir}/docker_output/<数据流分析结果文件名>
- 反编译源码目录: <从输入任务中继承的反编译源码目录绝对路径>

## 任务说明
基于数据流分析结果，对照反编译源码，进行深度安全漏洞挖掘。

### 数据流分析文件
请完整阅读以下数据流分析文件：{working_dir}/docker_output/<数据流分析结果文件名>

### 源码目录
请在以下目录中查找和审计源码：<从输入任务中继承的反编译源码目录绝对路径>
```

> **重要**:
> - 此文件将作为最终漏洞挖掘阶段（带 AI 评审闭环）的输入任务
> - `反编译源码目录` 必须从输入任务中原样继承，这是漏洞挖掘阶段读取源码的唯一路径
> - `数据流分析文件` 路径必须指向 docker_output 中的实际文件
> - 所有路径必须使用绝对路径
