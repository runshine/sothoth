apiVersion: apps/v1
kind: Deployment
metadata:
  name: secflow-app-code-server
  namespace: secflow-ns
spec:
  replicas: 1
  selector:
    matchLabels:
      name: secflow-app-code-server
  template:
    metadata:
      labels:
        name: secflow-app-code-server
    spec:
      serviceAccountName: secflow-app-code-server-serviceaccount
      dnsPolicy: ClusterFirstWithHostNet
      containers:
      - name: secflow-app-code-server
        image: ghcr.io/runshine/secflow-app-code-server:latest  # 包含kubectl命令
        imagePullPolicy: Always
        ports:
        - containerPort: 8080
        volumeMounts:
        - name: secflow-app-code-server-config
          mountPath: /app/config.yaml
          subPath: config.yaml
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /api/app/code-server/health
            port: 8080
          initialDelaySeconds: 30
          periodSeconds: 30
        readinessProbe:
          httpGet:
            path: /api/app/code-server/health
            port: 8080
          initialDelaySeconds: 30
          periodSeconds: 30

      volumes:
      - name: secflow-app-code-server-config
        configMap:
          name: secflow-app-code-server-config
          items:
            - key: config.yaml
              path: config.yaml
