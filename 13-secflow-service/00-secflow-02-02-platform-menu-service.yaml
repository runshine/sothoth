apiVersion: v1
kind: Service
metadata:
  name: secflow-platform-menu
  namespace: secflow-ns
spec:
  type: ClusterIP
  selector:
    name: "secflow-platform-menu"
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
      name: secflow-platform-menu