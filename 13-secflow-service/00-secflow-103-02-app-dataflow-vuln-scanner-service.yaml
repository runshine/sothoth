apiVersion: v1
kind: Service
metadata:
  name: secflow-app-dataflow-vuln-scanner
  namespace: secflow-ns
spec:
  type: ClusterIP
  selector:
    name: "secflow-app-dataflow-vuln-scanner"
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
      name: secflow-app-dataflow-vuln-scanner
