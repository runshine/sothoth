apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: secflow-platform-redis-data-pvc
  namespace: secflow-ns
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 10Gi
