apiVersion: v1
kind: Service
metadata:
  name: secflow-app-secmate-ng
  namespace: secflow-ns
spec:
  type: ClusterIP
  selector:
    name: "secflow-app-secmate-ng"
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
      name: secflow-app-secmate-ng