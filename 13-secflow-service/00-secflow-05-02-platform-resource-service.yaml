apiVersion: v1
kind: Service
metadata:
  name: secflow-platform-resource
  namespace: secflow-ns
spec:
  type: ClusterIP
  selector:
    name: "secflow-platform-resource"
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
      name: secflow-platform-resource