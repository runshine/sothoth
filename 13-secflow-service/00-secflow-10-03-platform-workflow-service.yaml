apiVersion: v1
kind: Service
metadata:
  name: secflow-platform-workflow
  namespace: secflow-ns
spec:
  type: ClusterIP
  selector:
    name: "secflow-platform-workflow"
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
      name: secflow-platform-workflow