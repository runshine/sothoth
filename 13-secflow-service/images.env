#!/bin/bash
# 镜像配置（供 deploy.sh 渲染 YAML 使用）

export SECFLOW_PLATFORM_RESOURCE_IMAGE="${SECFLOW_PLATFORM_RESOURCE_IMAGE:-ghcr.io/runshine/secflow-platform-resource:latest}"
export SECFLOW_PLATFORM_RESOURCE_FILE_GATEWAY_WORKER_IMAGE="${SECFLOW_PLATFORM_RESOURCE_FILE_GATEWAY_WORKER_IMAGE:-ghcr.io/runshine/secflow-platform-resource-file-gateway-worker:latest}"
export SECFLOW_APP_FIRMWARE_UNPACKER_IMAGE="${SECFLOW_APP_FIRMWARE_UNPACKER_IMAGE:-ghcr.io/runshine/secflow-app-firmware-unpacker:latest}"
