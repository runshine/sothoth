apiVersion: v1
kind: Service
metadata:
  name: secflow-platform-agent
  namespace: secflow-ns
spec:
  type: ClusterIP
  selector:
    name: "secflow-platform-agent"
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
      name: secflow-platform-agent