apiVersion: apps/v1
kind: Deployment
metadata:
  name: secflow-app-binary-security
  namespace: secflow-ns
spec:
  replicas: 2
  selector:
    matchLabels:
      name: secflow-app-binary-security
      role: api
  template:
    metadata:
      labels:
        name: secflow-app-binary-security
        role: api
    spec:
      dnsPolicy: ClusterFirstWithHostNet
      containers:
        - name: secflow-app-binary-security
          image: ghcr.io/runshine/secflow-app-binary-security:latest
          imagePullPolicy: Always
          env:
            - name: TZ
              value: Asia/Shanghai
            - name: SECFLOW_BINARY_SECURITY_CONFIG
              value: /app/config.yaml
            - name: SECFLOW_BINARY_SECURITY_ROLE
              value: api
            - name: SECFLOW_BINARY_SECURITY_ENABLE_SCHEDULER
              value: "false"
            - name: CONFIG_PATH
              value: /app/config.yaml
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: POD_IP
              valueFrom:
                fieldRef:
                  fieldPath: status.podIP
          ports:
            - containerPort: 8080
          volumeMounts:
            - name: secflow-app-binary-security-config
              mountPath: /app/config.yaml
              subPath: config.yaml
            - name: secflow-platform-fileserver-data-nfs-pvc
              mountPath: /data
          resources:
            requests:
              memory: "256Mi"
              cpu: "200m"
            limits:
              memory: "1Gi"
              cpu: "1000m"
          livenessProbe:
            httpGet:
              path: /api/app/binary-security/health
              port: 8080
            initialDelaySeconds: 30
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /api/app/binary-security/ready
              port: 8080
            initialDelaySeconds: 15
            periodSeconds: 15
      volumes:
        - name: secflow-app-binary-security-config
          configMap:
            name: secflow-app-binary-security-config
            items:
              - key: config.yaml
                path: config.yaml
        - name: secflow-platform-fileserver-data-nfs-pvc
          persistentVolumeClaim:
            claimName: secflow-platform-fileserver-data-nfs-pvc
