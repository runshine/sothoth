apiVersion: v1
kind: Service
metadata:
  name: secflow-app-dataflow-analyse
  namespace: secflow-ns
spec:
  type: ClusterIP
  selector:
    name: "secflow-app-dataflow-analyse"
    role: api
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
      name: secflow-app-dataflow-analyse
