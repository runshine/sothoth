apiVersion: apps/v1
kind: Deployment
metadata:
  name: external-service-nginx-proxy
  namespace: sothothv2-ns
  labels:
    app: external-service-nginx-proxy
spec:
  replicas: 2
  selector:
    matchLabels:
      app: external-service-nginx-proxy
  template:
    metadata:
      labels:
        app: external-service-nginx-proxy
    spec:
      containers:
        - name: nginx
          image: nginx:1.21-alpine
          ports:
            - containerPort: 80
              name: http
          resources:
            requests:
              memory: "256Mi"
              cpu: "250m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          volumeMounts:
            - name: external-service-nginx-proxy-config
              mountPath: /etc/nginx/nginx.conf
              subPath: nginx.conf
          livenessProbe:
            httpGet:
              path: /healthz
              port: 80
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /healthz
              port: 80
            initialDelaySeconds: 5
            periodSeconds: 5
      volumes:
        - name: external-service-nginx-proxy-config
          configMap:
            name: external-service-nginx-proxy-config
            items:
              - key: nginx.conf
                path: nginx.conf