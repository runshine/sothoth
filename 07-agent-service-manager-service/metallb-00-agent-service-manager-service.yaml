apiVersion: v1
kind: Service
metadata:
  name: agent-service-manager-service
  namespace: sothothv2-ns
  annotations:
    metallb.universe.tf/allow-shared-ip: "true"  # 可选：允许IP共享
spec:
  type: LoadBalancer
  selector:
    name: "agent-service-manager-service"
  ports:
    - port: 8082
      targetPort: 80
      protocol: TCP
      name: agent-service-manager-service
  loadBalancerIP: "192.168.12.90"  # 必须在static-ips-pool范围内