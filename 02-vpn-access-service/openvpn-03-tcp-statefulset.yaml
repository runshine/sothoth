apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: sothothv2-vpn-access-openvpn-tcp
  namespace: sothothv2-ns
spec:
  serviceName: openvpn-tcp-vpn-access-service
  replicas: 3
  selector:
    matchLabels:
      app: openvpn-tcp-vpn-access
  minReadySeconds: 1
  template:
    metadata:
      labels:
        app: openvpn-tcp-vpn-access
    spec:
      # 亲和性设置 - 避免同一应用的所有副本都在同一节点
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              podAffinityTerm:
                labelSelector:
                  matchExpressions:
                    - key: app
                      operator: In
                      values:
                        - openvpn-tcp-vpn-access
                topologyKey: kubernetes.io/hostname

      # 容器定义
      containers:
        # tcp容器
        - name: openvpn-tcp
          image: kylemanna/openvpn:latest
          imagePullPolicy: IfNotPresent
          command: [ '/script/entrypoint.sh' ]
          env:
            - name: TCP_MODE
              value: "true"
            - name: NET_ADDRESS
              value: 200.64.0.0/16
          ports:
            - containerPort: 11194
              name: server
              protocol: TCP
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "500m"
          volumeMounts:
            - name: openvpn-script-vpn-access-config
              mountPath: /script
            - name: openvpn-certfile-vpn-access-config
              mountPath: /etc/openvpn
            - name: openvpn-ccdfile-vpn-access-config
              mountPath: /etc/openvpn/ccd
            - name: dev-net-tun
              mountPath: /dev/net/tun
          livenessProbe:
            exec:
              command:
                - /bin/bash
                - /script/healthcheck.sh
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 5
          readinessProbe:
            exec:
              command:
                - /bin/bash
                - /script/healthcheck.sh
            initialDelaySeconds: 5
            periodSeconds: 5
          securityContext:
            capabilities:
              add:
                - NET_ADMIN

      # 卷定义
      volumes:
        - name: openvpn-script-vpn-access-config
          configMap:
            name: openvpn-script-vpn-access-config
            defaultMode: 0755

        - name: openvpn-certfile-vpn-access-config
          configMap:
            name: openvpn-certfile-vpn-access-config
            defaultMode: 0555

        - name: openvpn-ccdfile-vpn-access-config
          configMap:
            name: openvpn-ccdfile-vpn-access-config
            defaultMode: 0555

        - name: dev-net-tun
          hostPath:
            path: /dev/net/tun
            type: CharDevice