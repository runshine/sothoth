apiVersion: v1
kind: Service
metadata:
  name: client-script
  namespace: sothothv2-ns
spec:
  type: ClusterIP
  selector:
    name: "client-script"
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: client-script