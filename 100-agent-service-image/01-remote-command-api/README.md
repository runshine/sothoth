# 远程调试工具容器

## 功能特性

1. **Web API接口**：提供HTTP API执行远程命令
2. **完整调试环境**：包含gcc、g++、clang、gdb、strace、ltrace等工具
3. **特权容器**：具有系统级访问权限
4. **主机目录访问**：主机根目录只读挂载到`/host`
5. **命名空间共享**：除挂载命名空间外，与主机共享其他命名空间
6. **超时控制**：默认180秒超时（可配置）
7. **安全控制**：危险命令过滤

## 快速开始

### 构建镜像
```bash
docker-compose build