我在设计一个对目标环境的全面而详细的监测工具，用来做系统的安全监测，想通过tegragon实现，请针对下面的监控内容，给出一个详细的设计方案，相同进程要求实现有5分钟窗口的去重机制，不同进程不需要去重（考虑到事件可能非常多，请设计高性能的方案），最后结构化上报到ES数据库中；给出Dockerfile和docker compose的部署文件
进程执行（Process Execution）：监控新进程的创建，包括命令行参数、工作目录、用户ID等信息。
文件操作（File Operations）：监控文件的打开、读取、写入、关闭等操作，可以跟踪到具体进程和文件路径
网络活动（Network Activity）：监控网络连接
系统调用（System Calls）：仅跟踪敏感的系统调用
能力变化（Capability Changes）：监控进程权限（例如，通过 setuid、setgid 等）的变化。
命名空间变化（Namespace Changes）：监控进程命名空间的创建和切换。
信号传递（Signal Delivery）：监控进程接收到的信号（如 SIGKILL、SIGTERM 等）。
进程退出（Process Exit）：监控进程的退出，包括退出代码和信号。
内核模块加载（Kernel Module Loading）：监控内核模块的加载和卸载。
特权提升（Privilege Escalation）：监控进程权限提升的行为，例如通过 setuid 二进制文件。
文件系统挂载（Filesystem Mount）：监控文件系统的挂载和卸载操作。
内存相关事件（Memory Events）：例如，内存分配、映射等。
要求为：
1、不要在docker-compse中部署es，我要上传到远端的es中，通过环境变量的方式进行传入
2、针对不同的事件，需要设计不同的去重机制，去重的范围在相同的PID内，不同的PID不需要去重，同时不同的事件也有有不同的去重参数，如进程执行使用命令+参数去重，文件使用文件名+操作方式去重，请详细设计并实现；
3、增加远程动态配置的API接口（如rest接口），运行在进程运行时操作，允许远程开启或者关闭去重机制，也允许远程开启或者关闭某一类事件的监控；
4、提供API用来查询统计信息；
5、监控时需要去除监控进程、包括去重进程产生的所有事件；
6、不需要提供监控指标端点
7、将主机的网卡br-sothoth的IP、主机名、以及从宿主机目录上/sothothv2/config/sothothv2_agent.ini解析获取uuid，作为key同时附属到事件上，该文件的内容如下：
root@develop-linux:~# cat /sothothv2/config/sothothv2_agent.ini
workspace=/sothothv2
project_id=111111111111111111
server_addr=192.168.12.90
server_port=80
uuid=849fff18-879dffa6-1d40bbc0-6a528d21
log_level=info
log_path=/sothothv2/log/sothothv2_agent
foreground=false
8、该镜像需要同时在aarch64、amd64、armhf、armel上运行
9、给出tetragon的配置文件
10、给出完整的go的项目文件内容，包括go.mod等，go语言的代码在一个文件中，不要用多个文件
11、给出文件夹结构
