apiVersion: v1
kind: Service
metadata:
  name: static-binary
  namespace: sothothv2-ns
spec:
  type: ClusterIP
  selector:
    name: "static-binary"
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: static-binary